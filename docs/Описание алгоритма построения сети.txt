1. Пройти по всем узлам, если это эллипс - найти все связи, выходящие из него, добавить нейрон с id узла +'|' +коэффициентом связи на сенсорный слой.
text нейрона = text узла + вес связи


2. Для каждого нейрона моторного слоя выполнить:

	2.0. Найти все связанные с ним узлы. Для каждого из них запустить рекурсивную функцию traverse с параметрами 
	index = 0, weigth = вес связи, sender = нейрон сенсорного слоя которая соответствует связи между узлом с id 
	нейрона сенсорного слоя и текущим узлом
		
	Функция traverse:	
		2.1. Если тип узла - прямоугольник - Проверить количество скрытых слоев сети, если оно меньше index + 1, то добавить новый скрытый слой
			
			2.1.0 И определить количество связей узла
			
				2.1.1. Если количество связей == 0 - 
				Проверить, нет ли на моторном слое нейрона с id = id узла + 'motor'
				Eсли нет, то создать нейрон на моторном слое, 
				создать нейрон на скрытом слое с номером index. Создать связь между нейроном, который вызвал функцию traverse 
				и нейроном скрытого слоя, присвоить связи вес weight. Создать связь между нейроном скрытого слоя и нейроном моторного слоя.
				присвоить связи вес, который соответствует коэффициенту узла-прямоугольника.
				
				Если нейрон есть, то найти нейрон скрытого слоя с id = id узла. Создать связь между нейроном, который вызвал 
				функцию traverse и нейроном скрытого слоя, присвоить связи вес weight.
				
				
				2.1.2. Если количество связей > 0 - Проверить, нет ли на скрытом слое с индексом index нейрона с id = id узла.
				Если нет, то создать нейрон на скрытом слое с индексом index, создать связь между нейроном, 
				который вызвал traverse и нейроном скрытого слоя, задать вес связи = weight. Для всех узлов, связанных с текущим, 
				вызвать функцию traverse, передав в неё параметр index + 1 и weight = коэффициент узла-прямоугольника, 
				sender = нейрон скрытого слоя.
				
				Если нейрон на скрытом слое уже есть, то создать связь между нейроном sender и нейрономом скрытого слоя, присвоить связи 
				вес = weight
		
		2.2. Если тип узла - эллипс, то для всех узлов, связанных с ним, вызвать функцию traverse, передав в неё параметр
		index, weight, sender
		